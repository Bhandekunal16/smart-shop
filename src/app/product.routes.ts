import { Routes } from '@angular/router';
import { UpdateProductComponent } from './update-product/update-product.component';

export const ProductRoutes: Routes = [
//   { path: '', children: [{ path: '', component: UpdateProductComponent }] },
];
